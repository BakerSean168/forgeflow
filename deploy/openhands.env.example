# OpenHands Agent Server configuration. Copy to /etc/forgeflow/openhands.env (0600).
SESSION_API_KEY=
OH_SECRET_KEY=
# Provider credentials are injected per execution by ForgeFlow; do not put model API keys here.
